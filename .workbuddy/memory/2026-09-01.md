# 2026-09-01

## P0 推进

### #4 serial_test（P0 #4）— 提交 `287c187`
- 加 `serial_test = "3"` dev-dep；将所有读/写 `std::env::*` 的测试标 `#[serial]`。
- 删 `src/test_env.rs` 里的 `ENV_LOCK` / `lock()`（保留 module 作 placeholder）。
- 受影响模块：`tests.rs` / `client/bridge.rs` / `commands/session.rs` /
  `runtime_paths.rs` / `transport/tunnel.rs`。
- `runtime_paths::with_env_many` 去掉内部 `env_lock()`（由 `#[serial]` 兜底）。
- 默认测试 705/706 → 全绿 + 一个沙箱 `KERN_PROCARGS2 EPERM`（已知）。
- `native-ssh` 测试 861/862 → 全绿 + 一个沙箱 `KERN_PROCARGS2 EPERM`（已知）。

### #3 真 daemon（P0 #3）— 提交 `1428336`
- 新增 `src/transport/ipc/server.rs`：从原 `ipc/daemon.rs` 测试 mod 里把
  `serve_one` / `dispatch` 提到公开模块；wire payload 类型 `pub(crate)` 给 client 复用。
- `Operation::Challenge`：daemon 回送 nonce = Tier-1 liveness proof（无 PID / 无平台代码）。
- `commands/transport_daemon.rs`：`run` 保持 feature-off 拒绝；新增 `run_with`
  真接 `Config::from_env()` → `open_transport` → `server::run`。
- `main.rs` 的 `TransportDaemon` 子命令接到 `run_with`。
- 门：fmt ✅ clippy ✅ × 2（default + native-ssh），test 707 passed / 2 failed
  （沙箱 EPERM，已知非回归）。

### 升版 v1.1.2 — 提交 `fb3c9d4` + tag `v1.1.2`
- `Cargo.toml`: `1.1.1` → `1.1.2`；`CHANGELOG.md` 补条目。
- `cargo publish --allow-dirty` 上 crates.io ✅；`git tag -a v1.1.2` ✅；
  `git push origin main && git push origin v1.1.2` ✅；
  `gh release create v1.1.2 --notes-file <段>` ✅。

## P0 推进（续）

### #2 Tier-1 真接线 — 提交 `f342240`
- `NativeTransportClient::challenge()`：发 `Operation::Challenge`，解 `ChallengeAck`，用 2s deadline。
- `apply_socket_timeouts(stream, timeout)`：连接时设 `SO_RCVTIMEO`/`SO_SNDTIMEO`，
  把 kernel-level timeout 加到每一次 read/write 上。wedged daemon → `EAGAIN` →
  `DaemonUnavailable`（同于对端 hung up）。
- `daemon_lifecycle::challenge_via_ipc(state) -> bool`：连 `ipc_endpoint` → 读 `token_path` →
  Hello → Challenge → 比 nonce。任一失败返 `false`（让 assess 走 Tier 2）。
- `tunnel::decide_stop` 把 `|_nonce| false` 替换为 `|nonce| challenge_via_ipc(state)` 闭包
  （feature + unix 门控）。无 native-ssh / Windows → 维持原占位。
- 测试：happy / nonce mismatch / 错 token / 缺 endpoint / 缺 nonce / dead endpoint /
  30s hung daemon 不能阻塞超过 2s deadline（用 10s 上限保证 CI 友好）。
- 默认测试 707/709 ✅ + 2 failed（沙箱 EPERM，已知）。
- `native-ssh` 测试 719/720 ✅ + 1 failed（同 EPERM）。
- 关键：socket 路径**必须短**——`/tmp/vcli-c-<uuid>.sock`，不能用 `std::env::temp_dir()`，
  macOS 下 `/var/folders/...` 路径轻松破 `sun_path` 104 字节。

## 关键设计决定（要记住）

- `ipc::daemon` 与 `ipc::server` 现在都门控为
  `#[cfg(all(unix, any(test, feature = "native-ssh")))]`：测试无需 feature
  即可跑，生产无 feature 则不编译入 binary。`server::run` 额外再加
  `#[cfg(feature = "native-ssh")]`（仅 daemon 子命令调用）。
- `server::run` 当前**永不返回**（loop accept forever），daemon 进程的 graceful
  shutdown 由 step 6 引入；本步只在编译期保证 run_with 成功 bind 即视为 daemon 启动成功。
- **✅ Tier-1 真接线已完成**：`assess(state, challenge_via_ipc)` 在 unix+native-ssh
  下走真 IPC，nonce 一致即 `Alive`；任何失败均 fall through 到 Tier 2。
- Unix UDS 路径上限 104 字节（macOS/Linux `sun_path`）；测试 / 真实 daemon
  都需用短路径（设计里的 fallback 方案：hash 名 + 固定短目录）。

## step 4/6 推进（下午，提交 8a61848 / 097eabc / f09641a / 82b18a2 / 2e9968a）

- **step 4a**（`8a61848`）：`transport/scheduler.rs` —— Priority 三级（Urgent/Normal/Bulk）、
  SchedulerLimits（total/bulk/urgent_reserve，validate 拒绝 total < bulk+reserve、
  reserve==0）、SessionScheduler（condvar FIFO、urgent 插队、bulk 饿死宽限后升 Normal、
  deadline 过期 → QueueTimeout retryable）。25 测试。
- **step 4b**（`097eabc`）：Config 新增 `ssh_max_sessions`/`ssh_max_bulk_sessions`；
  `SchedulerLimits::from_config` + `validate_capacity(config)`，接进 `open_transport`
  native 分支（不变量 `total >= spectre_max_workers + 2`，违反 = Configuration 错误，
  OpenSSH 路径不受影响）。
- **step 4c**（`f09641a`）：`transport/pool.rs` —— EndpointKey（全连接属性，
  Debug 规范形 SHA-256 取 12 hex 当 digest）、EndpointPool::get_or_create
  （RwLock 快路径，digest 匹配复用、失效重建、并发单次工厂调用）、Endpoint::acquire
  （permit + deadline 组合）。16 测试。
- **step 6a**（`82b18a2`）：`transport/lifecycle.rs` —— FailureClass 三分类
  （Permanent=host-key/auth/proxy/policy；Transient=connection/daemon/jump/local-io；
  RequestLevel=queue-timeout/remote-exit/unknown-outcome/cancelled，**永不触发重连**）、
  ReconnectPolicy（1s 基数翻倍封顶 30s + splitmix64 确定性 ±25% jitter）、
  CircuitBreaker（连续瞬态失败 N 次 → Degraded，success 只清计数不清 Degraded，
  只有显式 reset 能清）、CancellationToken（AtomicBool 协作式）、
  ShutdownCoordinator（三阶段：停准入 → grace 内轮询静止 → 恒执行取消）。
- **step 6b**（`2e9968a`）：Config 新增 5 字段 + env 接线：
  VB_SSH_RECONNECT_MAX_ATTEMPTS=8 / VB_SSH_RECONNECT_MAX_DELAY=30 /
  VB_SSH_KEEPALIVE_INTERVAL=30 / VB_SSH_KEEPALIVE_FAILURES=3 /
  VB_TRANSPORT_SHUTDOWN_GRACE=10（zero：reconnect/keepalive 拒绝，grace 接受）。
  常量挂在类型上（DEFAULT_*）防漂移。23 测试。
- **基线问题**：`tests/config.rs::test_config_timeout_default` 在本机恒失败
  （`load_dotenv_upward()` 从上层目录读到 VB_TIMEOUT=60），已用 stash 验证与本次改动无关。
- **待办 6c**：native transport 消费这些原语（russh keepalive+重连、daemon 关停三阶段、
  SKILL RetryPolicy::Never / OutcomeUnknown 不重放）。然后 step 7/8（CI 矩阵 + 文档 + stable）。

## step 6c — native transport 真正消费 lifecycle 原语（全部完成）

### 6c-1 keepalive + 建立期重试 — 提交 `2d13710`
- `NativeTransportConfig` 增 `keepalive: KeepalivePolicy` / `reconnect: ReconnectPolicy`；
  直通 russh `client::Config { keepalive_interval, keepalive_max }`（clippy 要求
  struct update 语法 `..Default::default()`）。
- `establish_with_retry`：仅 `FailureClass::Transient` 且建立期（connect+auth，
  未发操作字节 → 不可能重放）重试；退避 `jittered_delay(attempt, seed).min(deadline.remaining())`。
- `from_config` 走两个 policy 的 `from_config`（零值构造期报错）。3 新测试，7/7。

### 6c-2 SKILL RetryPolicy::Never + OutcomeUnknown — 提交 `165942f`
- `error.rs` 新变体 `OutcomeUnknown(String)`（error_type "outcome_unknown"，刻意不进 `retryable()`）。
- `bridge.rs` 新 `RetryPolicy::{Never, IdempotentProbe}`；核心 `execute_skill_with_policy`；
  sync_N 票据分支：Never → `OutcomeUnknown`，Probe → 预算内 `continue` 重发。
- 新 `execute_skill_idempotent_probe`（bypass + Probe）；`test_connection` / `daemon_alive` /
  `get_daemon_version` / `get_daemon_user` + tunnel.rs / version.rs / rpc/dispatcher.rs
  的固定只读字面量全部切 probe。
- mock-daemon（TcpListener 127.0.0.1:0 脚本化应答）3 测试证明：Never 不发第二次连接；
  Probe 耗尽 stale ticket 后成功。注意 `execute_skill` 有 auth 门，测试要直调
  `execute_skill_with_bypass`。

### 6c-3 daemon 关停走 ShutdownCoordinator — 提交 `6cd8eeb`
- `ipc/server.rs`：`ShutdownState{token, active, coordinator}` + `ActiveGuard`（Drop 计数-1）；
  `serve_one_with_shutdown`：进 dispatch loop 计数+1、loop 顶查 token、
  **Shutdown 分支先 cancel 后写 ack**（客户端见 ack 即证明已 fire）。
- `run`：非阻塞 accept + 25ms 轮询 token；退出后三阶段
  `coordinator.execute(drop(listener), active==0, ||{})` → unlink socket。
- `daemon.rs` 新 `NativeTransportClient::request_shutdown()`（Operation::Shutdown，5s deadline）。
- `transport_daemon.rs::run_with` 从 Config 建 coordinator 传入。

### 6c-3 调试教训（重要）
- 🔴 **macOS 上 accept 出来的 socket 继承 listener 的 O_NONBLOCK** → `run` 把 listener
  设非阻塞轮询后，worker 线程 `read_frame` 立即 `WouldBlock` → 连接秒断 → 客户端
  `DaemonUnavailable`。修复：accept 后 `stream.set_nonblocking(false)`。
  这解释了为什么单测快速失败（0.14s）而非 5s 超时。
- Shutdown 分支 ack-before-cancel 是真竞态（非测试 flake）：客户端可在服务端线程
  cancel 前看到 ack。修成 cancel-then-ack。
- 门禁：fmt ✅、clippy ×2 ✅、ipc 23/23（default）+ 25/25（native-ssh）、
  全量 native-ssh 805/805 ✅、default 788+1 个 `KERN_PROCARGS2` EPERM（已知基线）。

### 6c 剩余 / 下一接缝
- 6c-4（可选）：`tunnel stop` Tier-1 证明 daemon 存活时优先 `request_shutdown()` 优雅关停，
  信号作 fallback。
- 队列：step 4 收尾（单文件 SFTP streaming）→ step 7（三平台 CI 矩阵）→ step 8（诊断/迁移文档/stable）。

### 6c-4 tunnel stop 优先 IPC 优雅关停 — 提交 `f6effc2`
- `daemon_lifecycle::shutdown_via_ipc(state)`（feature 门控）：连接后**先重做 nonce challenge**
  （endpoint 可重绑，不盲信 decide_stop 早先的判定）再 `request_shutdown()`；
  返回 ack。`daemon.rs` 的 ack 在 cancel token 之后写 → 见 ack 即证 admission 已停。
- `tunnel.rs`：新 `StopDecision::GracefulIpcShutdown`；`decide_stop` 在 native 两阶判定
  到 `Verdict::Alive`（此路径只能来自 IPC challenge）→ 返回该变体，Tier 2 仍映射 Signal。
- `stop_saved_tunnel` 新臂：调 `shutdown_via_ipc`，再 `wait_for_daemon_exit(pid, grace)`
  （`kill(pid,0)`→ESRCH 判退出，超时 = 未退），失败/超时回退 `signal_tunnel_pid`。
  非 native 配置该变体不可达，自然降级为 signal，match 仍穷尽。
- `wait_for_daemon_exit` unix+native-ssh 辅助：`let mut stopped` 按配置拆 `mut`/`let`
  （默认配置不赋值会 `unused_mut`）。
- 测试：tunnel `ipc_proven_daemon_yields_the_cooperative_shutdown_decision`（spawn 一次性
  `serve_one` daemon 断言决策）+ `wait_for_daemon_exit_*`（走了 pid 立即、活 pid 超时，
  均不 spawn 外部进程，沙箱安全）；daemon_lifecycle `shutdown_acks_*`/`shutdown_refuses_*`。
- 门禁：fmt ✅、clippy ×2 ✅、daemon_lifecycle 19/19 + tunnel 30/30（native-ssh）、
  全量仅剩已知 `KERN_PROCARGS2` EPERM 基线 flake（identity 测试，非回归）。

## step 6c 全部收尾（6c-1 `2d13710` / 6c-2 `165942f` / 6c-3 `6cd8eeb` / 6c-4 `f6effc2`）

下一接缝可选：**6c-5** 把 `tunnel status` 的 alive 判定也走 Tier-1 IPC challenge（与 stop
对称），或进 step 4 收尾（SFTP streaming）。用户未点名 6c-5，继续则从 step 4 开始。

## step 4 收尾 SFTP streaming（单文件）— 提交 `134ba36`

- `Cargo.toml`：`[features]` native-ssh 增 `russh-sftp`；`[dependencies]` 加
  `russh-sftp = { version = "2", optional = true }`（解析 2.3.0，要求 russh ^0.62.5
  与已锁 0.63.1 兼容）。注意 `cargo add russh-sftp --optional --features client` 失败
  （"unrecognized feature client"），故手写依赖行。
- `src/transport/native.rs`：
  - `open_sftp(cfg, deadline)`：establish → `channel_open_session` →
    `request_subsystem(true,"sftp")`；子系统拒绝或 `SftpSession::new` 失败映射为
    `UnsupportedOperation`（供 caller fallback 识别）。返回 `(Handle, SftpSession)`。
  - `upload_via_sftp` / `download_via_sftp`：`SFTP_CHUNK = 64*1024` 流式窗口，
    `sftp.create/open` → 按 chunk `write_all`/`read`；错误映射 `TransferInterrupted`。
  - `upload_file` / `download_file` 改为 SFTP 优先 + exec `cat` fallback：
    `match ... { Ok => Ok, Err(e) if sftp_unavailable(&e) => exec cat, Err(e) => Err(e) }`。
    `bytes.clone()` 传入 SFTP（exec fallback 复用原 `bytes`）。
  - `sftp_unavailable(e)`：`matches!(e, UnsupportedOperation(_))`；只在 sftp 子系统缺失时
    fallback，连接/auth/传输错误不进 exec 管道。
  - 单测 `sftp_unavailable_routes_only_to_the_exec_fallback`（UnsupportedOperation→true；
    ConnectionFailed/AuthenticationFailed/TransferInterrupted→false）。
  - 模块文档更新："single files stream over the SFTP subsystem ... with an exec `cat`
    fallback ... directories keep tar-over-exec"。
- **门禁**：fmt ✅、clippy ×2（default+native-ssh，`-D warnings`）✅、
  native 单测 8/8 ✅（含 sftp_unavailable）、全量 lib native-ssh **811 passed / 0 failed**
  （本次 KERN_PROCARGS2 EPERM 偶发 flake 也通过；后台任务状态 "failed" 仅因沙箱拦截
  `~/.ssh/known_hosts`/`config` 读，代码已优雅降级，非回归）。
- 提交只含 `Cargo.toml` + `src/transport/native.rs`（排除 window.rs / x11.rs / resources /
  .claude 等无关改动）。推送 `f6effc2..134ba36`。
- **SFTP 端到端**留给 step 7 CI 矩阵（sftp server fixture，本沙箱起不了 sftp server）；
  本 increment 仅编译级 + `sftp_unavailable` 路由单测验证。

queue: step 7（三平台 CI 矩阵）+ step 8（诊断/迁移文档/stable 发布）。

## step 7 跨平台集成矩阵 — 提交 `04a1f5e`

- 新 `.github/workflows/integration.yml`：matrix `os=[ubuntu/macos/windows-latest]` ×
  `features=[default, native-ssh]`，每组合 `cargo build` + `cargo test`，`fail-fast:false`。
  这是设计 step 7 的结构性集成网，也是**首次在 CI 编译 `identity.rs` 的 windows 分支**
  （P1 最高风险项）。
- `ci.yml` `check` 任务加 `cargo clippy --features native-ssh -- -D warnings`（ubuntu，
  本地已验证绿），让 feature 在主线 pipeline 也被 lint。
- **表达式坑已修**：初版 `matrix.features == 'default' && '' || format(...)` 对 `default`
  会错误注入 `--features default`（因 `'' || format` 走右支）。改为
  `matrix.features != 'default' && format('--features {0}', matrix.features) || ''`
  （default→空串不注入；native-ssh→`--features native-ssh`）。
- 矩阵只跑 build+test（clippy 留 ubuntu），避免 Windows lint nit 误红整个矩阵。
- YAML 结构已校验（两文件均含 `jobs:`，GitHub `${{}}` 表达式在 YAML 中是普通字符串）。
  无法在本机跑 GitHub runner，推到 origin 后由 Actions 实际验证（尤其 Windows native-ssh 构建）。
- **step 7 剩余（7b）**：SFTP 端到端 fixture（进程内 russh-sftp server 或系统 sshd），
  供 step 4 真 SFTP roundtrip；矩阵已是承载它的框架。

queue: step 7b（sftp fixture）/ step 8（诊断+迁移文档+stable 发布）。

## step 7b 真 SFTP roundtrip 集成测试 — 提交 `7a0f589`

- **做法**：russh-sftp 自带 client+server，无需真 sshd/网络。测试用
  `tokio::io::duplex` 把 `SftpSession` 接到一个最小化内存 `Handler`
  （`MemFs`：`files: HashMap<filename,Vec<u8>>` + `handles` 映射），三平台
  CI 都能跑（step 7 矩阵已承载）。
- **重构（零行为变更）**：把 `upload_via_sftp`/`download_via_sftp` 的
  create/open + chunk 循环抽成 `sftp_write_all` / `sftp_read_all`
  （`russh_sftp::client::fs::File` 参数）；测试与生产共用同一条代码路径。
- **覆盖**：payload 200 KB > `SFTP_CHUNK`(64 KiB) → 强制多 chunk 写/读；
  断言字节级一致。`sftp_unavailable` 路由单测仍保留。
- `Cargo.toml`：tokio 加 `io-util`（测试用 `tokio::io::duplex`；此前经 russh
  feature 统一已开启，现显式）。
- **查 russh-sftp 2.3.0 API**：`Handler` 用 RPITIT（`async fn` 实现，
  `type Error = StatusCode`，有 `From<StatusCode> for StatusReply`）；
  client `create`/`open` 不发 realpath（只发 OPEN+WRITE/CREAT/TRUNC），
  故 server 只需 init/open/write/read/close（+ fstat/stat/realpath 防御性实现）；
  `File` 是 `Unpin` 且 impl `AsyncRead`/`AsyncWrite`。`MemFs` 用 std `Mutex`
  字段、方法内不持锁 await（避免 clippy `await_holding_lock`）。
- **clippy 坑**：`FileAttributes::default()` 后 `attrs.size = Some(len)` 触发
  `field_reassign_with_default` → 改为
  `FileAttributes { size: Some(len), ..Default::default() }`。
- **门禁**：fmt ✅、clippy native-ssh ✅（`-D warnings`）、clippy default
  后台跑（native.rs 整文件 feature 门控不进 default，唯一 default 改动是
  tokio `io-util` 标志，必过）、native 测试 9/9 ✅（含 roundtrip）。
- **不能 `#[tokio::test]`**：tokio 未开 `macros`；测试用既有 `make_runtime()`
  + `rt.block_on(async {...})` 跑（current-thread runtime 上 `tokio::spawn`
  服务端任务协作调度，duplex 双向流驱动）。

step 7 收尾（7a 矩阵 `04a1f5e` + 7b roundtrip `7a0f589`）。queue: step 8（诊断/迁移文档/stable）。

## step 8a `tunnel status` SSH backend diagnostics — 提交 `15c5ce6`

- `src/commands/tunnel.rs`：抽 `pub(crate) fn backend_diagnostics(cfg, state_backend)
  -> (Value, Value, Option<String>)` 返回 `config.backend.{selected, supported_in_build}`
  / `tunnel.backend` / drift warning。`status()` 在 IO 块前后调用并合并进 JSON；
  无 live tunnel 时只填 `config.backend`（不写 `tunnel.backend`，JSON key
  缺失而非 null 表达"不适用"）。
- `selected` 严格来自 `cfg.ssh_backend`，未知值（如 `banana`）显式
  `supported_in_build: false` —— 设计禁止静默回退到 openssh。
- `supported_in_build` 用 `cfg!(feature = "native-ssh")` 镜像编译期开关，
  让 openssh-only build 的 native 请求在 status 一目了然，而不是等到真正
  transport 调用时撞 `UnsupportedBackend`。
- drift warning：`config.selected != on_disk` 时 `Some("backend drift: ...")`
  → `result["config"]["backend_warning"]`。
- 6 个单测（无 env/无 IO）：default/native+feature/native+no-feature/banana/
  state_openssh_vs_config_openssh/state_native_vs_config_openssh(state_openssh)/
  state_native_vs_config_native。
- 门禁：fmt ✅、native-ssh clippy ✅（6m09s, 0 warning）、default clippy ✅
  （3m10s, 0 warning）、6/6 单测绿；提交仅 `src/commands/tunnel.rs` (+169)。
- 已推 `7a0f589..15c5ce6`。

queue: step 8b（迁移文档：README 双语增 SSH backend selection + 能力差异表）→
step 8c（stable 发布：v1.2.0 + crates.io + gh release）。

## step 8b README SSH 后端选择迁移文档 — 提交 `8fb4422`

- README 新增 `### SSH Backend Selection`（英文） + `### SSH 后端选择`（中文），
  位置对称在 SSH Remote Connection Setup 之后、How It Works 之前。
- Key Features / 核心特性 各加一行"可选的纯 Rust 原生 SSH 后端"。
- 内容：默认 OpenSSH / `VB_SSH_BACKEND=native` 显式切换 / 无自动迁移
  / feature 门控 / `tunnel status` JSON 诊断指针（接 8a）/ 能力差异矩阵
  13 行（host_keys / agent / password / keyboard-interactive / ProxyJump /
  ~/.ssh/config -F / 单文件 SFTP / dir transfer / 并发复用 / 自动重连 /
  keepalive / SOCKS5 / RAMIC-X11 forward）/ 迁移指引（"depends on right-col no:
  stay openssh; single-hop or self-contained: opt into native"）。
- 表格 "否" 项一一对照代码：native.rs L19-21（模块顶部 ❌ 清单）、
  L241-249 / L601（`ProxyJump not implemented` / `requires VB_SSH_KEY:
  agent, password, keyboard-interactive`）、L857（不读 ssh_config）。
- 验收：README 围栏 90 个偶数 ✓、英文/中文 selection 段各 50 行对称 ✓、
  每 env var / 子命令 / flag 回 Config / TunnelState / main.rs 核实 ✓、
  `git diff --check README.md` 干净。
- 仅 `README.md` +100 行，无关改动全部排除。
- 已推 `15c5ce6..8fb4422`。

queue: step 8c（stable 发布：v1.2.0 + CHANGELOG + Cargo.toml 升版 + crates.io + tag + gh release）。

## step 8c v1.2.0 stable 发布 — 提交 `121309c`

- `Cargo.toml` 1.1.2 → 1.2.0；`CHANGELOG.md` 顶部增 v1.2.0 条目（89 行），
  覆盖 step 4 收尾/6c/7a/7b/8a/8b，标记 native backend "stable for
  single-hop direct-connection workloads that fit the README capability matrix"。
- 门禁：fmt ✅、native-ssh clippy ✅（2m20s, 0 warning）、default clippy ✅
  （1m52s, 0 warning）；`git diff --check` 干净。提交仅含
  `Cargo.toml` + `CHANGELOG.md`（+90/-1）。
- 发布链：`cargo publish --allow-dirty` dry-run ✅ → 正式 publish ✅ →
  `crates.io` API 验证 `max_stable=1.2.0, newest=1.2.0, downloads=806`。
- git push origin main `8fb4422..121309c` ✅ → `git tag -a v1.2.0`
  + `git push origin v1.2.0` ✅ → `gh release create v1.2.0` ✅
  （release notes 来自 CHANGELOG v1.2.0 段，89 行）。
- 设计文档 step 8 "publish diagnostics, migration documentation, and
  stable status" 闭环。

## step 8d release.yml macOS/Windows 二进制 — 提交 `3c03a09`

- `release.yml` 加 `build-macos-aarch64`（`macos-latest`，Apple Silicon runner）与
  `build-windows-x86_64`（`windows-latest`，MSVC toolchain）两个 job，与现有
  `build-linux-x86_64` 结构同构；只 build default feature（不带 `native-ssh`），
  三平台产出 `vcli` + `vtui` + `virtuoso-daemon`。
- `publish-crate.needs` 加这两个 job → crates.io 发布前必等 4 个平台 build 完成。
- YAML 结构验证：5 jobs 全部就位（`STRUCT_OK`）；`git diff --check` 干净。
- 已推 `121309c..3c03a09`。下次 v* tag（如 v1.2.1 / v1.3.0）触发时 gh release
  会有 macOS aarch64 + Windows x86_64 assets；v1.2.0 release 保持仅 Linux 二进制
  （这是历史事实，不可改——gh release assets 一旦发布不能 retroactive 上传）。
- 不在 release build 加 `--features native-ssh`：避免 release runner 上为
  russh / russh-sftp 引入 vendored OpenSSL bindings 显著拖慢 Windows/macOS build；
  要 native 后端的用户在客户端显式 `cargo install virtuoso-cli --features native-ssh`。

## 交付完成总览

设计文档 step 0–8 全部在 `origin/main`：

- step 0/1/2/3/4(scheduler+pool)/4(SFTP)/5(jump)/6(identity+stop)/6c 全接缝
- step 7a 三平台 CI 矩阵 `04a1f5e` + 7b 真 SFTP roundtrip `7a0f589`
- step 8a tunnel status 诊断 `15c5ce6` + 8b README 迁移文档 `8fb4422`
  + 8c v1.2.0 stable 发布 `121309c`

`virtuoso-cli v1.2.0` 现已在 crates.io + github tag + gh release 同步。
OpenSSH 始终默认；`VB_SSH_BACKEND=native` 显式启用 native 后端，
无自动迁移。Native backend 文档化为 stable（single-hop 直连范围）。

## step 8g 设计文档 Status 标注 — 提交 `bb58daa`

- 设计文档顶部插入 `## Status — ✅ Delivered, published as v1.2.0 (stable)` 段：
  逐 step 0–8 交付状态表 + "Explicit deltas from this design" 子段。
- **如实标注范围边界**（避免误述为全交付）：
  - step 5（SOCKS5 + jump）❌ Deferred —— native 单跳，`ProxyJump` 返回
    `UnsupportedOperation`，SOCKS5 标 "planned"
  - step 6 ⚠️ Partial —— reconnect/lifecycle/SKILL 不重放 ✅；**RAMIC/X11
    forward 未实现**（native 返回 no，README 矩阵已记）
  - 设计 Purpose 第 3 条（SOCKS5 应用级代理路由）native 未满足，由 OpenSSH 满足
- 与设计文档 L535（"首 stable 含 SOCKS5"）的偏差显式列 delta，符合项目
  "偏离设计必须标 delta，不静默改行为" 约定。
- 零行为变更，doc/spec only；`git diff --check` 干净、围栏 20 偶数不变。
- 已推 `3c03a09..bb58daa`。同步固化 MEMORY.md：新增"已知范围边界"长期段，
  待办 P1#5 标记 8a/8b/8c/8d/8g 全完成。
- **设计 step 0–8 主线至此全部闭环于 origin/main；剩余 8e（cargo-audit）/
  8f（dependabot）为可选安全/依赖治理增强，非设计文档强制项。**

## step 8e 依赖漏洞扫描 CI — 提交 `99c1e59`

- 新增 `.github/workflows/security-audit.yml`：`rustsec/audit-check@v2.0.0`
  跑 `cargo audit`。trigger：`push` 匹配 `**/Cargo.toml`（lockfile 被 gitignore
  故 Cargo.lock 路径对主仓是 no-op，留作 fork 兼容）+ 周 `schedule`（周一
  03:37 UTC，避 :00 洪峰）+ `workflow_dispatch` 手动。
- 权限按官方 README 收窄为 `contents: read` / `issues: write` / `checks: write`；
  push/PR 发现 advisory 即红 check，schedule 另开 issue。
- 已核实 action tag `v2.0.0` 与 token 用法（WebFetch 官方 README）。零行为变更，CI-only。
- 本 push 不触发该 workflow（paths 不匹配新增 yaml）；本地并行 `cargo install
  cargo-audit` 预扫依赖（后台 `Dlv7H9`，待结果报 advisory）。
- 已推 `bb58daa..99c1e59`。
- **8f dependabot 提交 `e0c1c33`**：`.github/dependabot.yml` v2，cargo +
  github-actions 双生态、每周、10 PR 上限、dependencies 标签。已 WebFetch
  GitHub Docs 核实 schema（version 2，cargo 不需提交 Cargo.lock）。仓 gitignore
  lock，Dependabot 读 Cargo.toml 开 PR 并在 PR 内生成 lock。已推 `99c1e59..e0c1c33`。
- **cargo-audit 本地预扫失败（环境限制，非漏洞）**：`cargo install cargo-audit`
  成功（~19min），但 `cargo audit` 拉 RustSec/advisory-db 报 "error sending
  request" + 502 CONNECT tunnel。探针确认：crates.io API 200 可达；github.com
  HTML 超时(000)、advisory-db git smart-http 502。→ 本沙箱出口策略拦 github，
  本地无法预扫；CI 8e 在 GitHub runner 正常。**不能**给出"0 advisory"结论，
  权威结果以 CI run 为准。已修正 MEMORY.md 网络事实段。
- **8g2 范围边界回归测试 提交 `2710537`**：native.rs 加两个测试——
  `establish_rejects_jump_host_with_unsupported_operation`（jump host 在
  `establish` 连接期返回 `UnsupportedOperation`，fail-fast 不触网，锁 step 5
  Deferred）+ `native_transport_does_not_implement_local_forward`（forward 走
  trait 默认 `UnsupportedOperation`，锁 step 6 Partial）。native 测试 11/11 通过、
  双配置 clippy 零警告。发现并记录：缺 key 实际返回 `Configuration`（非
  `UnsupportedOperation`），README/设计文档措辞不精确，已在 MEMORY.md 标 🔴 修正。
