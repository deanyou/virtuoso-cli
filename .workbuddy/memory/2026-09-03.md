# 2026-09-03

## PR / Issue review（纯分析，未改代码）

### 事实清单（已核实，对照 origin/main @ b18081a）

- **唯一 open PR = #62**（`fix/dismiss-minimized-focus`，+12/-0 仅 py）。CI 双 run
  (33694185328 / 33694185320) 卡在 `in_progress`，`updated_at` 停在 2026-09-02T23:14:50，
  `mergeable_state=unstable`，无 review。需 rerun。
- main 最新提交 b18081a 的 CI（run 33693961349）4 job 全 success。
  ⚠️ `gh run list` 的 status 会陈旧，判断 CI 必须 `gh run view <id> --json status,conclusion,updatedAt`。

### 🔴 僵尸 issue（代码已修，issue 未关）
| issue | 实际修复 | 证据 |
|---|---|---|
| #30 symlink 死代码 | PR #33 `O_NOFOLLOW` | x11.rs:1586-1603 已用 `custom_flags(libc::O_NOFOLLOW)`；:1564 `symlink_metadata` |
| #34 display 未回填 | PR #39 | `parse_window_list` x11.rs:529-539 回填 display；action_x11 走同一函数 :952 |
| #35 xdotool 兼容 | PR #39 + #42 | 全仓库已无 `--sync`/`--button`；chord 归一化；回归测试 :3629/:3665/:3830 |
| #47 dialog 几何启发式 | PR #51 | py `_semantic_dialog_classification` :428 + 6 tests |
| #48 NotFound 误报 Conflict | PR #51 | x11.rs:1011/1034 已改 `VirtuosoError::NotFound` |

注：PR #51 body 写 `Closes #46, #47, #48`，但只有 #46 自动关闭 → 逗号分隔的 closing keyword 未生效，
以后应一行一个 `Closes #N`。

### 仍真实未修的 issue
- #50 与 #53 重复（同一主题：SKILL window reflection 太浅，IC25.1 `hiGetCurrentDialog` undefined）→ 合并到 #50。
- #54 缺操作：`X11Operation` 枚举仅 8 项（x11.rs:645-654），无 absolute click / minimize / maximize / double-click。
- #55：`--pid` 在 main.rs:1517 是 `u32`（必填且必须 > 0），window-id 无法单独定位 → 描述属实。
- #49：CLI 不一致属实（`DismissWindowX11.window_id` positional main.rs:1488 vs `ActionX11` `#[arg(long)]` :1513）。
- #11 CentOS7 glibc 2.28（2026-08-29 外部用户提，无跟进、无 label）。

### process 观察
- 9-02 21:25→23:14 连续 7 个 PR（#52→#56→#57→#58→#59→#60→#61）修同一「GUI 边界」主题，
  典型链式：修一条路径 → 合入后立刻发现相邻路径同病。粒度偏细，验收面只覆盖改动路径。
- #51 带 +6 py 测试；#61/#62 零测试（纯逻辑改动无回归保护）→ process regression。

### 本地工作区
- 分支 `fix/x11-list-windows-empty` 落后 origin/main **13 个提交**（HEAD=270b15f，main=b18081a）。
- 未提交改动 `src/transport/x11.rs` +29 行 = 「list-windows exit 1 视为健康」，
  **已被 origin/main 的 PR #41 完全覆盖**（main 已有同名测试
  `list_windows_exit1_empty_means_no_windows_not_error` x11.rs:3011、
  `list_windows_real_failure_exit2_surfaces_helper_error` :3039，注释措辞几乎一致）
  → 属于重复劳动，建议 `git checkout -- src/transport/x11.rs` 丢弃，不要提交。

## 安装 virtuoso-gui-debug 技能（`.claude/skills/` → 用户级）

源：`virtuoso-cli/.claude/skills/virtuoso-gui-debug/`（Claude Code 约定目录，WorkBuddy **不**自动发现）。
安装到 `~/.workbuddy/skills/virtuoso-gui-debug/`（rsync，排除 `__pycache__`），
frontmatter 仅多一行 `agent_created: true`；其余文件与源完全同步。

### 安全审计结论：P2 安全，可安装
- 唯一外部执行面 = `command_runner.py`：`subprocess.run(list, shell=False)`，argv 元素拒 NUL/换行，
  子进程 env 只保留 PATH/LANG/LC_ALL + `VCLI_CAPABILITY=admin`（**不传 DISPLAY / VB_***）。
- SSH 适配器只执行固定 vcli argv（校验 `argv[0] == vcli_path` + `shlex.quote`），
  hostname 走 `^[A-Za-z0-9][A-Za-z0-9._-]*$`。
- 无 eval/exec/网络/pickle/base64；`os` 仅用于 flock（`~/.cache/virtuoso_bridge/x11-locks/display_<N>.lock`）。
- 179 tests OK（82s，managed python 3.13.12）。

### 审计中发现并已修正的 doc-vs-code 偏差（源 + 安装副本都已改）
| 位置 | 原文 | 实际 |
|---|---|---|
| Output Files 表 | `locks/` 在 DIR 下 | 实为 `~/.cache/virtuoso_bridge/x11-locks/display_<N>.lock`，跨 run 共享 |
| Constraints | 「nothing is written outside it」 | 假：DISPLAY 锁写在 `--output` 之外 |
| Usage | 未提 `--fake-outcomes` | fake 默认全 SUCCESS，不注入 outcome 则 `fail.json` 也 passed |
| Constraints | 「Live runs require fresh `--output`」 | 用 `mkdir(exist_ok=False)`，**fake 同样**拒绝复用目录 |

### 易踩
- `--output` 必须是**不存在**的目录，先 `mkdir -p` 反而会 `FileExistsError` → exit 2。
- fake 注入 outcome 的 JSON 字段：`step_id/phase/attempt/outcome`，
  precheck/baseline 用伪 id `_precheck`/`_baseline`（见 `engine.py:104/109`）。

## 安装 virtuoso-skill-dev 技能（git/skill 仓库 → 用户级）

源：`/Users/dean/Documents/git/skill/.claude/skills/virtuoso-skill-dev/`（240 文件 / 10 MB）
→ `~/.workbuddy/skills/virtuoso-skill-dev/`（rsync，排除 `__pycache__`，**未改任何文件内容**）。
冒烟通过：Layer1 validate ✅ / Layer2 AST validate 检出 4 错（AST201×2 / AST301 / AST302）。

### 审计：无恶意代码，但 2 个 P1 本地攻击面（用户选择「原样安装」，**风险已接受**）
- 无 shell=True（全部 list argv）；无 eval/exec/网络监听/curl|bash；7 个 .sh 均为常规 setup/test；
  `.github/workflows/test.yml` 是标准 pytest+flake8；12 个 .pkl 已用 `pickletools` 反汇编验证，
  只引用 `skill_ast.*` AST 节点类，**非 payload**。
- 🟠 **P1-a `/tmp/skill-debug-$USER.sock` 无认证 exec 服务**（`tools/skill_debug_daemon.py`）
  - 实测 socket 权限 `srwxr-xr-x`（0755，umask 022）→ **任意本地用户可 connect**；
    无 peer-cred 校验，收 `{"cmd":"exec","expr":"..."}` 即 `vcli skill exec`（带 `VCLI_CAPABILITY=admin`）。
  - `scripts/debug_wrapper.py:_ensure_daemon()` 会 detached 自启（`start_new_session=True`），空闲 30 min。
  - 另有 /tmp socket 劫持面 + `_cleanup()` 不可靠（实测进程退出后 `.sock`/`.pid` 残留）。
- 🟠 **P1-b pickle 缓存目录随 cwd 漂移**（`tools/ast_cache.py:114/319` 默认 `cache_dir=".skill_cache"`）
  - 实测：`Layer2Validator().cache.cache_dir` 解析为「运行时的 cwd」/`.skill_cache`（mktemp 目录验证）。
  - 即：在含恶意 `.skill_cache/metadata.pkl` 的目录里跑 `debug_wrapper.py validate --layer2`
    → `pickle.load` 任意代码执行。`tools/.skill_cache/`（12 个 pkl）本身就是历史上从 tools/ 跑出来的残留。
- 附带：`lsp_server/clients/vscode/*.vsix`（458 KB 预编译扩展）未深审，属惰性资源（不装进 VSCode 不执行）。

### 待办（用户未授权，未做）
- 修 P1-a：socket 移到 `~/.cache/` 下 0700 目录 + 0600 权限 + 校验 `LOCAL_PEERPID` == 自身 uid。
- 修 P1-b：`cache_dir` 默认改为绝对路径（如 `~/.cache/skill-ast-cache`）。

### 用户级技能现状
`~/.workbuddy/skills/` = chrome__skillhub / minimax-docx / playwright-browser-automation /
pr-review-hardening / spec-vs-code-audit / **virtuoso-gui-debug** / **virtuoso-skill-dev**。
两个 virtuoso 技能都是「源在 repo 的 `.claude/skills/`，复制一份到用户级」→ 存在双份漂移风险。

## 第二轮 PR review（15:00，纯分析，未改代码）

### 🔴 main 持续红（04:40 起，ci.yml 的 Check & Lint 连续 4+ 次 failure）
- 失败步骤 = `cargo fmt --check`（第 5 步），导致后面 3 个 clippy 步骤**全被 skipped**
  → clippy 门禁实际已失效 ~10 小时。
- 唯一 13 处违规（本地 worktree 复核）：`src/commands/window.rs:425/439/469`
  + `src/transport/x11.rs:697/818/1632/1966/2723/2738/2745/2769/2779/2804`。
- 引入提交（**全部直推 main、无 PR**）：`bab1809` action-x11-batch、`513f929` --direct、`75e139f` scroll。
- **被掩盖的第三颗雷**：CI 里 clippy 三步被 fmt 跳过，本机补跑
  `cargo clippy --all-targets -- -D warnings` 发现
  `error: this function has too many arguments (8/7)` → `src/transport/x11.rs:1514 action_x11_batch`
  → 即 main 上 **fmt 与 clippy 两道门禁实际都是红的**，只有 Test job 绿。
  修法：按 `ActionX11Inputs` 的既有模式把 8 个参数打包成一个 struct。
- 查 CI 真相必须 `gh run view <id> --json jobs --jq '.jobs[]|.steps[]'`；`gh run list` 的 status 会陈旧
  （曾把已 success 的 run 显示成 in_progress 8 小时）。
- 复核 main 的办法：`git worktree add /tmp/vcli-main origin/main --detach`，
  再 `CARGO_TARGET_DIR=<主仓>/target cargo fmt --check`（复用 target 缓存）。

### 🔴 PR #66（open，CentOS7 glibc 2.17，修 issue #11）两颗雷
1. **E0433 `cannot find 'sys' in crate root`**（Build/Test/Lint 三 job 全红）：
   只在 `src/lib.rs` 加了 `#[cfg(target_os="linux")] pub mod sys;`，**漏了 `src/main.rs`**
   —— 本仓 bin（vcli/vtui）是独立 crate，各自 `mod commands;` 再编一遍 commands，
   所以 bin 侧没有 `sys`。⚠️ 新增模块必须**同时**注册到 lib.rs 和 main.rs。
2. **修完第 1 颗雷会撞第 2 颗**：`rename_noreplace` 是 safe fn，而 `src/sys/linux_rename.rs`
   的测试用 `unsafe { rename_noreplace(...) }` 包裹 → `unused_unsafe` → clippy `-D warnings` 再红。
   （rustc 本地已复现：`fn safe_fn(p:*const i32)` 被 `unsafe{}` 包 → `unnecessary unsafe block`）
3. 🟠 soundness：该函数收裸指针却是 safe fn，原 `unsafe { libc::renameat2(..) }` 边界被移除
   → 应标 `pub(crate) unsafe fn`。
4. 🟠 fallback 用 `libc::stat64`（跟随链接）判存在 → dangling symlink 目标会被 renameat 覆盖，
   违背 RENAME_NOREPLACE 语义 → 应用 `lstat64`。
5. 🟡 `Err(..)` → `return Err(..);` 是非必需改动（rustc 验证块发散规则成立、类型安全，纯 noise）。
6. 🟡 PR body 勾了 "cargo check --tests passes" 与 CI 事实不符：macOS 本地 `#[cfg(target_os="linux")]`
   整段被裁 → **本地假绿**，linux 上必挂。cfg-gated 代码在 macOS 上验证无效。

### 🟠 已合并 PR / 直推提交的问题
- **#63**（discover_windows title 走 xdotool getwindowname fallback）：每个 title 为空的窗口
  fork 一次 xdotool → 与刚做的 P0 性能优化（--direct、batch 合并成一条 shell）**方向相反**；
  list-windows 变慢会连带拖慢 wait 轮询。零测试。
- **#64**（`_parse_window_line` 取"最后一个含引号的括号组"当 class）：窗口标题含带引号括号时
  （如 `Virtuoso (editing "inv")`）会误判 class + 截错 title。零测试。
- **#65**（virtuoso-gui-debug skill，18 文件 +1453）体量大，本轮未逐行审。

### 🟠 action-x11-batch / --direct 的风险（直推 main，无 PR 审查）
- `--direct` 路径**没有 MouseButtonGuard**：drag-rel 的 mousedown→mousemove→mouseup 三步，
  中间任一步 `runner.run_command(...)?` 返回 Err 就整体提前返回 → **mouseup 永不执行、鼠标键卡住**。
  与之前 review #5 引入 MouseButtonGuard 的初衷相悖。（batch direct 用 `;` 串联，不受此影响）
- `action_x11_batch`（window.rs:416 + x11.rs:1514）**零单测**；同批 scroll 有 7 个单测，覆盖不均。
- batch 超时语义被悄悄放大：`total_timeout = timeout_secs * prepared.len()`，无法对单条命令限时。
- ✅ 已核实安全：argv 全部 `shell_escape`；SSH transport 用 `Command::new(ssh).args(..)`
  + **stdin 写入命令**（`ssh.rs:376-379`），不经本地 shell → batch 的 `echo __RC_a_c:$?` 在远程展开，无注入。

## 执行修复（15:22 起，Agent 模式）

### Task 1 — main 门禁（PR #67 已开，CI 验证通过）
- 工作树 `/tmp/vcli-main`（detached @ origin/main 8513a84）→ 分支 `fix/main-gate-batch-fmt` → 提交 `478cfc6` → PR #67。
- 改动：`src/transport/x11.rs` 新增 `BatchX11Inputs<'a>` struct，把 `action_x11_batch` 的 8 参数降为 4（清 `clippy::too_many_arguments`）；`src/commands/window.rs` 调用处先构造 struct。`cargo fmt` 顺带清 13 处违规（来自无 PR 直推的 bab1809/513f929/75e139f）。
- **本地验收**：`cargo fmt --check` ✅、`cargo clippy --all-targets -- -D warnings` ✅（7m34s，无 warning）、`cargo test` 862 passed / 2 failed。
  - 2 failed = `identity` / `daemon_lifecycle` 的 `sysctl KERN_PROCARGS2 errno 5 (EPERM)` —— **本沙箱 macOS 限制，与改动无关**，CI runner 上绿。
- **CI 验证**：run 33730500557（CI workflow）`Check & Lint`=success、`Build`=success、`Test`=success → main 红门禁已救活。

### Task 2 — PR #66 CentOS7 glibc 修复（已推送，等 Linux CI）
- 工作树 `/tmp/vcli-pr66`（detached @ origin/fix/centos7-glibc217 cbb0c4c）→ 提交 `e6e39ee`（detached HEAD）→ `git push origin HEAD:fix/centos7-glibc217` 更新 PR #66（注意：worktree 是分离 HEAD，必须用 `HEAD:branch` 推，普通 `git push` 报 up-to-date 但不生效）。
- 4 处修复：
  1. `src/main.rs` 补 `mod sys;`（解决 E0433：lib.rs 有 `pub mod sys` 但 bin crate 没有，bin 独立编 commands 找不到 `crate::sys`）。位置按 rustfmt 排序放在 `mod streaming;` 之后。
  2. `src/sys/linux_rename.rs` `rename_noreplace` 由 safe fn 改 `pub(crate) unsafe fn`（soundness，收裸指针）。所有调用点（maestro.rs / 单测 / 集成测试）本就包在 `unsafe{}` 内 → 无 `unused_unsafe`。
  3. fallback 的 `libc::stat64` → `libc::lstat64`（dangling symlink 不再被覆盖，保 NOREPLACE 语义）；同步改函数注释。
  4. 顺带修 PR #66 自身预先存在的 fmt 违规：`src/lib.rs` 的 `pub mod sys;` 被放在 `#[cfg(test)] pub mod test_env;` 之后，违反 rustfmt 模块排序、导致 Check & Lint 必红（被 build 先挂掩盖）。移到 `test_env` 之前。
- **本地验收**：`cargo fmt --check` ✅；`cargo check --all-targets --offline`（独立 target + 复用 registry 缓存，避免 crates.io 联网失败）✅ 通过 → 证明无语法/结构错误。
  - ⚠️ Linux 专属逻辑（cfg(target_os="linux")）在 macOS 本地是**假绿**，E0433 是否真解 + unsafe 调用是否正确 **只能靠 Linux CI**（run 33731230906 Integration Matrix 含 ubuntu-latest/default）。macOS 本地只验证语法。
- 复现注意：`cargo check` 用全新 `CARGO_TARGET_DIR` 会触发 crates.io index 更新并 HTTP2 层失败（`curl failed [16]`）→ 用共享 target + `--offline` 解决。

### 待收尾
- PR #67 的 Security Audit job 可能仍因 `lru 0.12.5` 两条 unsound（RUSTSEC-2026-0002/0253）红灯（记忆记载，与本次改动无关，属预存问题）。
- PR #66 的 Linux CI 结果待确认。

### Task 3 — `--direct` drag 补 MouseButtonGuard（PR #69 已开，全绿）
- **根因**（review 第二轮 🟠）：`--direct` 快速路径（x11.rs:1000-1087）直接 `for (_sub,argv) in cmds { runner.run_command(...)? }`，
  drag-rel 的 mousedown→mousemove→mouseup 三步中任一步 `?` 提前返回 → mouseup 永不执行 → **鼠标键卡在目标窗口**。
  非 direct 路径早已用 `MouseButtonGuard`（x11.rs:1339），direct 路径漏了。
- 工作树 `/tmp/vcli-pr67`（branch `fix/direct-drag-mouseguard` @ origin/main `4019331`）→ 提交 `2d5fe1a` → PR #69。
- 改动（`src/transport/x11.rs`，仅 affects `DragRel` direct 分支，其余 direct 操作 click/key/type/scroll 不变）：
  - 把 direct 路径的 drag 执行拆成 `if operation == DragRel { 带 guard 的 for 循环 } else { 原 ? 循环 }`。
  - guard 用法完全镜像非 direct：`MouseButtonGuard::new(runner, &display_prefix, &resolved, *button)`；
    `i==0` 成功 → `guard.arm()`；`i==2` 成功 → `guard.mark_released()`；中途 `Err` → `return Err`（Drop 仍发 best-effort `mouseup --window <wid> <btn>`）。
  - **确认** `build_xdotool_actions` 的 DragRel 顺序固定为 `[mousedown(i0), mousemove_relative(i1), mouseup(i2)]`，且 mouseup 带 `--window <wid>` → 与 guard Drop 的 `mouseup --window <wid> <btn>` 完全对齐（wid 取自 `resolved.window_id = window_id`）。
- **新增回归测试** `action_x11_direct_drag_issues_mouseup_on_failure`：新 mock `MidDragFailingTransport` 第 1 次调用 Ok（mousedown）、第 2 次 Err（mousemove）、第 3 次 Ok（guard 的 mouseup）；
  断言 action 返回 Err **且** 命令序列含 `mouseup --window 0x123 1`。无 guard 时该断言必败 → 真回归测试。
  - ⚠️ `TransportError` 无 `Io` 变体（只有 `LocalIo(String)`/`RemoteIo(String)`）；test 模块顶部 `use` 只导入了 `UploadTextRequest`，其余 request 类型需 `crate::transport::contract::` 全限定 → 初版因此报 E0599/E0425，已修。
- **本地验收**：`cargo fmt --check` ✅；`cargo clippy --all-targets -- -D warnings`（含 `--features daemon` / `--features native-ssh`）✅；`cargo test --lib x11` ✅ 104 passed（+1 新测试）。
- **CI 验证（双 run 全绿）**：CI run `33737643269` = success（Check & Lint / Test / Build / Security Audit 全 ✓）；
  Integration Matrix `33737643242` = success（ubuntu/macos/windows × default/native-ssh 全 ✓）。
- **状态**：PR #69 待人工 merge（AI 不开合并）。PR #66 仍待人工 merge（Linux CI 已绿，前轮已确认）。
- ⚠️ **工作区提醒**：用户真实工作区仍在 `fix/x11-list-windows-empty`（HEAD `270b15f`，落后 main 约 27 提交），
  且 `src/transport/x11.rs` 有 29 行未提交重复改动（已被 origin/main 的 PR #41 覆盖）→ 不可提交、应 `git checkout -- src/transport/x11.rs` 丢弃。
  Task 3 全程在 `/tmp` worktree 完成，未触碰用户工作区。

### Task 4 — PR #68 squash-merge + 工作区清理（用户输入 "继续" 后下一轮）
- PR #68 = `feat(x11): P2 geometry precheck, batch shared list-windows, geom cache`
  （branch `feat/p2-geometry-cache-scroll`），**10/10 CI 全绿（含 Windows）**。
- ⚠️ 初始 `mergeable=CONFLICTING`（mergeStateStatus DIRTY）：分支从旧 main 切出，与已合入 main 的
  PR #41/#67 在 `.claude/skills/virtuoso-gui-debug/SKILL.md` 文档块冲突；`x11.rs` 自动合并成功。
- 解决（工作树 `/tmp/vcli-pr68`）：`git rebase origin/main`（期间 main 推进 `ce57823`、分支在 `4966e42`）。
  唯一冲突 = SKILL.md 一处：HEAD(main) 已含带 "PR #68" 标注的 canonical P2 文档，incoming 是内部计划标签
  (P2-2/P2-3A/P2-3B) 版本 → **取 HEAD 版**，丢弃 incoming 重复。rebase 全 3 commits 成功。
- 验收：`cargo fmt --check` ✅；`cargo clippy --all-targets -- -D warnings`（含 daemon/native-ssh）✅；
  `cargo test --lib x11` ✅ 111 passed（PR #68 自带 geometry 测试）。
- `git push --force-with-lease origin feat/p2-geometry-cache-scroll` → `ec7eef3`；recheck `mergeable=MERGEABLE`。
- `gh pr merge 68 --squash --delete-branch` → **MERGED**，merge commit `5d1a55b`，`origin/main` 推进 `ce57823..5d1a55b`。
  （本地分支删除因 worktree 占用被跳过，远端分支已删，无碍）；`git worktree remove /tmp/vcli-pr68 --force` 清理。
- **工作区清理**：`cd /Users/dean/Documents/git/virtuoso-cli && git checkout -- src/transport/x11.rs`
  → 丢弃 29 行未提交重复改动（list-windows exit-1 块，已被 PR #41 覆盖）。
  ⚠️ 同工作区 `.claude/skills/virtuoso-gui-debug/SKILL.md` 的 uncommitted 改动**未动**（用户只点名 x11.rs）。
- **状态**：PR #66（CentOS7）、PR #69（direct drag guard）仍 open 待人工 merge；本轮只合了 #68。

### Task 5 — PR #66 + #69 全部 squash-merge（用户"继续合并"）
- **PR #66**（CentOS7 glibc，`fix/centos7-glibc217`）：rebase onto origin/main（`6f8710f`，自 #68 后仅有 docs 提交）→
  **6 commits 零冲突**。验收 `fmt` ✅ / `clippy -D warnings`（含 daemon/native-ssh）✅ /
  `cargo test --lib` 871 passed + **1 environmental fail**（`authorize_signal_succeeds_for_the_live_process`
  —— macOS 沙箱 `sysctl KERN_PROCARGS2` 全量跑时偶发 EPERM，隔离跑 PASS，**非回归**）。
  force-push → `1c4d49b`；`gh pr merge 66 --squash --delete-branch` → **MERGED** `ca4147b`，`origin/main=ca4147b`。
- **PR #69**（direct drag guard，`fix/direct-drag-mouseguard`）：rebase onto origin/main（`ca4147b` 即 #66 后）
  → **1 commit 零冲突**（尽管 #68 也动过 x11.rs，3-way 自动合）。验收 `fmt` ✅ / `clippy` ✅ /
  `cargo test --lib x11` 112 passed ✅。force-push → `1a8a391`；
  首次 `gh pr merge` 撞 "Base branch was modified" 竞态（main 刚被 #66 推进）→ **重试** → **MERGED** `832b644`，`origin/main=832b644`。
- 工作树清理：`/tmp/vcli-pr66b` `/tmp/vcli-pr69b` `/tmp/vcli-pr66` `/tmp/vcli-pr67` 全部 `worktree remove --force`
  （对应分支均已合并/删远端）。残留 `/tmp/vcli-main`（#67 已合）、`/tmp/ccb-*` 非本轮范围，未动。
- **总结**：今日 open 的 3 个代码 PR（#66/#68/#69）全部 squash-merge 入 main；#68 之前轮合，#66/#69 本轮合。
  工作区（fix/x11-list-windows-empty）x11.rs 重复改动已丢弃，未再做其他改动。

### Task 6 — 复核"残留 #62"（用户"顺手把残留的 #62 也重跑/合并"）
- **结论：#62 早已合并，无操作可做**。
- 实测：`gh pr view 62` → `state=MERGED`，`mergedAt=2026-09-02T23:19:38Z`，`mergeCommit=c05e5a3`
  （`fix(gui): restore minimized window before XSetInputFocus in dismiss_window (#62)`）。
  `git merge-base --is-ancestor c05e5a3 origin/main` → YES，确为 main 真祖先（非假合并）。
- 之前日志 line 7-9「唯一 open PR = #62，CI 卡 in_progress」是** stale 误记**：该 PR 在 9-02 夜即合并，
  9-03 的 review 仍按其"未合"状态判断，导致后续"残留 open #62"的推断全错。
- `gh pr list --state open` → 空。**当前仓库 0 个 open PR**，所有代码 PR（#56–#69 区间）均已合入 main。
- 本轮仅复核，未做任何改动；`origin/main` 现 `2f64ce2`（自 #69 后只多了 docs commit `2f64ce2` SVG 图表）。

### Task 7 — 对齐工作区 SKILL.md 到 origin/main（用户选方案 A）
- 背景：`git diff` 仅显示 19 增/2 删，但 `git diff origin/main` 暴露该文件本地是 **119 行裁剪版**（commit `bcf3982` 砍掉 xdotool local 模式/Playbook/Performance/API 语义），
  而 origin/main 是 **463 行完整版**（含 local 模式 + P2 优化，最后动于 #68 `5d1a55b`）。本地版还把 Live-Mode Contract 第5条锁路径错写成 `DIR/locks/`（main 已对，为 `~/.cache/virtuoso_bridge/x11-locks/`）。
  即本地版是过期且部分错误的草稿，非"小补丁"。那 19 行只是浮在上面的 3 处真实纠错（fake-outcomes 说明 + Output Files 锁路径 + Constraints 表述）。
- 用户选 **(A) 对齐 main**：`git checkout origin/main -- .claude/skills/virtuoso-gui-debug/SKILL.md` → 工作区现 463 行、与 origin/main 一致（git status `M` 已暂存未提交）。
- 安装副本 `~/.workbuddy/skills/virtuoso-gui-debug/SKILL.md` 仍停留在旧 119 行版（仅多 `agent_created: true` 一行），与仓库源现不一致 → 已向用户 offer 是否同步（未自动改，避免动到活跃技能）。
- 注意：此文件改动未提交，属于工作区未提交状态（与 x11.rs 已 `checkout --` 丢弃不同，这里是"换成 main 版本"故保留为改动）。



## Issue 全量盘点与关闭（2026-09-03 晚）

- 背景：**本地 git 严重落后于远程**。本地 HEAD=270b15f == 旧 main，而 origin/main=602e163（领先约 30 个 commit，含 PR #40–#69）。
  做 issue 状态判定前必须 `git fetch origin` 并以 **origin/main 的代码**为准，否则会拿过期工作区误判（本次差点把 #47/#48 判成未修）。
  判定手法：`git show origin/main:<path> > /tmp/om/<path>` 后用 Grep 工具检索，不要依赖本地工作区。

- 已关闭 5 个（均在 origin/main `602e163` 上验证代码已修）：
  - **#30** validate_png_artifact symlink 死代码 → `symlink_metadata` (x11.rs:1374) + unix O_NOFOLLOW (x11.rs:1393) + 测试 `validate_png_artifact_rejects_symlink` (x11.rs:3090)；PR #32 + #33。
    残留：dispatcher.rs:764 注释仍写 `execute_skill_unchecked`，实际调用 `execute_skill_idempotent_probe("plus(1 1)")` —— 纯文档 nit，已在关闭评论中记录，未自动改。
  - **#34** action-x11 display 未回填 → 统一走 `parse_window_list()`（x11.rs:660），action_x11 在 x11.rs:922 调用；回归测试 x11.rs:3753；PR #39。
  - **#35** xdotool 兼容 → `--sync` 移除（x11.rs:1178，测试 3244）、`--button` 改位置参数（x11.rs:2288，测试 3289/3309/3431）、连字符 chord 归一（PR #42）、README 补最低版本 3.20140419.1（README.md:79-85）；PR #39 + #42 + 602e163。
  - **#47** list-dialogs 纯几何启发式 → `_semantic_dialog_classification()`（x11_dismiss_dialog.py:438）先查 WM_TRANSIENT_FOR / _NET_WM_WINDOW_TYPE，几何退为 fallback；PR #51。
  - **#48** 不存在的 window-id 报 Conflict → 改 NotFound（x11.rs:1437-1446），Conflict 仅留给真多匹配（x11.rs:1426）；PR #51。

- 仍开放 5 个（代码侧确认未修）：
  - #49 dismiss-window-x11 仍是 positional `window_id: String`（main.rs:1487-1489），未接受 `--window-id`/`--pid` flag。
  - #50 / #53 **内容重复**（都是 SKILL 侧 window 反射太浅）。window_ops.rs:44 仍只产出 `{"name": ...}`，无 kind/mode/geometry/pid；`hiGetCurrentDialog` 仍在用（L52/54/61）。→ 建议把 #53 作为 #50 的 duplicate 关闭。
  - #54 缺操作：X11Operation 枚举现为 activate|key|type|click-rel|drag-rel|scroll|screenshot|wait|close —— 新加了 scroll/close，但 **absolute click / minimize / maximize / double-click 三个都没做**。
  - #55 `--pid` 仍是必填 `u32`（main.rs:1517-1518），Step 2 过滤硬要求 `w.pid == Some(*pid)` → 无 _NET_WM_PID 的窗口仍不可操作。

- 操作方式备忘：fish 下多行评论不可靠 → 先 Write 到 /tmp/cN.md，再 `gh issue comment N --body-file /tmp/cN.md`，最后 `gh issue close N`。

## 修复打开的 X11 issue（2026-09-03 晚，续）

分支 `fix/x11-open-issues`（基于 origin/main 602e163）。5 个 commit：
- `184d193` docs(rpc): dispatcher ping 注释修正（#30 残留，注释写 execute_skill_unchecked 但实际 execute_skill_idempotent_probe("plus(1 1)")）。
- `cc6f36f` fix #55：`--pid` 改 `Option<u32>`。`pid_matches(None)` 视作匹配任意，无 `_NET_WM_PID` 的窗口（pid:null）现在可达；无 window_id 时仍须给 pid。
- `7a5f33a` feat #54：新增 `click-abs`(mousemove -- 绝对坐标+click)、`minimize`(windowminimize)、`double-click`(click --repeat 2)、`maximize`(windowstate --add MAXIMIZED_HORZ/VERT)。**maximize 需 xdotool ≥ 3.20210804.1，刻意不抬高全局最低 3.20140419.1**（README 中英双语标注）。8 个新单测。
- `c34c5d5` feat #49：`dismiss-window-x11` 接受 `--window-id`（向后兼容位置参数）+ `--pid`（无 id 时按 pid 从 list-windows 解析，零/多匹配报 NotFound/Conflict）。RPC `window.dismiss_window_x11` 同样接受 pid。
- `0ad2cdd` fix #50：`WindowOps::list_windows` 增 `id`（`hiGetWindowId`），SKILL 侧枚举变可定位。**完整 kind/mode/geometry/pid 反射需活 Virtuoso 验证（hiGetWindowType/Mode/ScreenBox/ProcessId），保持 OPEN 在 #50**——不能凭记忆瞎写 SKILL 函数。

操作：issue 全部关闭（#55/#49/#54/#50 close，#53 作为 #50 duplicate 关闭）→ 仓库 0 open issue。push 分支 + 开 PR #70，CI 核心 gate（Build/Test/Lint/Check&Lint/ubuntu+macos default+native-ssh）全绿，Integration Matrix 在跑。

结论：**真实可修的都修了**；#50 的深层 SKILL 反射因本环境无活 Virtuoso 无法安全验证，留待有实例时补。

## #50 活 Virtuoso 实测（续，20xx，有实例了）

对用户级技能 `virtuoso-skill-dev` 提供的活 Virtuoso（SSH `ubuntu-docker:40939`→实际 daemon 在 docker 内 **动态端口 37787**，session `dean-user1-37787`）做了 `fboundp` 实测（execution 路径 `let`/`hiGetWindowList()` 在 daemon 上**偶发 flaky**，故只用 `fboundp` 判绑定，可靠）。

### IC25.1 实测函数可用性（fboundp）
- **bound**：`hiGetWindowList`(funobj)、`hiGetWindowName`(lambda)、`geGetEditCellView`(lambda)
- **unbound(nil)**：`hiGetWindowId`、**`hiGetWindowType`**、**`hiGetWindowMode`**、**`hiGetWindowScreenBox`**、`hiGetProcessId`、`hiGetWindowProcessId`、`dbGetWindowId`、`hiGetEditCellView`

### 结论
1. #50 的 kind/mode/geometry/pid 反射**在 IC25.1 上完全无法实现**——候选函数全 unbound。加进去会让 `vcli window list` 整体 `skill_ok()==false` 退化。启发式 `annotate_modes`(name 推导) 是唯一跨版本安全路径。原 deferral 正确。
2. **新发现回归**：PR #70 的 `id` 字段依赖 `hiGetWindowId`，PR body 称其为"稳定通用函数"，但 IC25.1 上它**也是 unbound** → `vcli window list` 在 IC25.1 直接报错（只在 IC23.1 等存在 `hiGetWindowId` 的版本可用）。这是真·版本兼容 regression，不是 deferral。证据已评论到 issue #50（comment 5526820606）。
3. **未改代码**：不写无法验证的 SKILL；跨版本安全的 id 取法需在 IC23.1 + IC25.1 双验证后方可动 `list_windows`，当前只凭 IC25.1 拿不到可靠 accessor（`w~>id` 因 daemon flaky 未证实）。

### 连活 Virtuoso 的环境坑（下次省时间）
- macOS `cache_root()` 走 `XDG_CACHE_HOME`（本机被设成 `/Users/dean/.cache`，且值带脏后缀）→ session 实际在 `/Users/dean/.cache/virtuoso_bridge/sessions/`，**不是** `~/Library/Caches`。`/Users/dean/.env` 注入 `VB_SESSION=dean-user1-45173`+`VB_REMOTE_HOST=222.211.247.16`（另一套环境）→ 必须 `VB_CACHE_DIR=/Users/dean/.cache VB_SESSION=dean-user1-37787` 显式覆盖。
- docker 的 `virtuoso-daemon` 以 `0.0.0.0 0` 启动→端口动态，session 文件名 `dean-user1-37787.json`(port 37787)。本地 `ssh -f -N -L 37787:localhost:37787 ubuntu-docker` 建隧道后，`vcli skill exec/eval` 即可连。
- `vcli skill exec` 不发 `--stdin`；读 stdin 用 `vcli skill eval --stdin`（包 `progn`）。`fboundp` 探函数存在比直接 `eval` 调用稳（execution 偶发 flaky）。
