# 2026-08-31

## 测试缺口审查（对照 main@806b662 + 隔离工作树 /private/tmp/vcli-ccb-p1-20260830）

产出：修订版待测清单（对话内），本轮**未修改源码或文档，未调用 CCB**。

### 我自己犯的错（已由用户纠正 + 我复核确认，勿再重犯）

1. **CLI stop 调用链是我编造的。** 我说 "CLI → stop_decision → openssh_stop_decision → Skip"，
   实际 `commands/tunnel.rs:46 stop()` 完全不碰 `stop_decision()`，它自己做
   `/proc` 检查 + `force` 绕过 + `taskkill` + 无条件 clear。
   → 教训：**断言调用链前必须 grep 实际调用点**，不能从"transport 里有个 stop_decision"反推 CLI 用了它。
2. **RemoteRoles 不是 0 测试，是 5 个**，在 `src/tests.rs:180-250`（fallback / 覆盖 / local mode /
   scratch / profile 优先级）。
   → 教训：本项目 Config 相关测试**集中在 `src/tests.rs`**，不在 `config.rs` 文件内。
   查"某模块有没有测试"必须**全仓 grep**，不能只看同文件 `#[cfg(test)]`。
3. **v1/v2 兼容测试存在**：`src/models.rs:500`（v1 解析）、`:521`（v2 往返）、`:561`（openssh save shape）。
4. **sshd fixture 优雅跳过分支存在**：`src/transport/testutil.rs:230-234`
   （`let Some(fixture) = SshServerFixture::start() else { eprintln; return; }`）。
5. **"身份三属性指向父进程"是错的**：`tunnel.rs:538-539` 里 `executable_path` / `start_identity`
   都是 `None`。方向（PID 生命周期不匹配）对，细节错。
6. **"安全 vs OpenSSH 可用性二选一"是错误二分。** 实施方案
   `2026-08-30-transport-p1.md:46` 明确：给新 OpenSSH 状态存身份；旧**活**进程无法验证→拒绝终止；
   旧进程**已证明消失**→允许清状态。不是拒绝所有 OpenSSH 状态。
7. **配置快照的测试目标我理解错了**：不是 `TunnelState` 字段完整性，而是
   "客户端创建后 env 变了，`stop()` 是否错用另一套凭据/跳板"。候选 `tunnel.rs:343` 仍重新
   调 `Config::from_env()`。

### 核实后仍成立 / 新发现（按严重度）

🔴 **A. CLI stop 与 transport stop_decision 是两条独立路径 —— 8/30 那批工作在 CLI 上是死代码**
- `main.rs:1522` → `commands::tunnel::stop(force, dry_run)`
- `transport/tunnel.rs:325 SSHClient::stop()` → 走 `stop_decision()`（`:333`）
- 两天做的 `daemon_lifecycle` / `identity` / `verdict_to_decision` **只接在 SSHClient 上**，
  用户实际敲的 `vcli tunnel stop` 完全没经过那套两级存活判定。
- 实施方案 Task 2 已列：`stop_saved_tunnel()` 接口 + "Remove duplicate command-layer kill/cleanup code"。

🔴 **B. macOS 上 `vcli tunnel stop` 从不杀进程，但状态被清掉（真实 bug，非缺测试）**
- `commands/tunnel.rs:87` 读 `/proc/{pid}/cmdline`；macOS **无 `/proc`**（实测 `ls -d /proc` → No such file）
- → `is_ssh = false` → 走 `:97-102` "skipping kill" 分支
- → 但 `:112` **无条件 `TunnelState::clear()`**
- 这是 memory 已记录 bug 的**第二份副本**：之前修的是 `transport/tunnel.rs::verify_ssh_pid`
  的同类 `/proc` 恒 false，`commands/tunnel.rs` 这个没一起修。

🟠 **C. `commands/tunnel.rs::stop()` 零测试覆盖** —— 该文件 18 个 `#[test]` 全是 `HostnameCheck`
（hostname mismatch warning / parse_gethostname_output / to_json_shape），无一测 stop。

🟠 **D. 其他行为问题（同一函数内）**
- `:92` `is_ssh || force` —— `force` 直接绕过验证
- `:105-110` 非 unix：`let _ = Command::new("taskkill")` 忽略结果，静默失败
- `:88-90` `c.contains("ssh")` 弱匹配，PID 复用后可能杀错进程
- `:72-83` **清状态前无条件做远端 cleanup**，与实施方案 `:48`
  "Do not clean remote files on stale/mismatched/unverifiable state" 冲突

✅ **E. 用户确认正确的部分**
- 生产路径确实未接 `backend::open_transport()`（Task 3 已列）
- 启动进程归属、Windows 完整验证、native/多平台验收仍有缺口
- 但"缺测试"不能直接定 P0；完整 OpenSSH 排除语义不是当前已承诺的验收要求

### 环境事实（补充）
- 隔离工作树 `/private/tmp/vcli-ccb-p1-20260830` 仍存在，实施方案在
  `docs/superpowers/plans/2026-08-30-transport-p1.md`（Task 2/3 的 checkbox 全未勾选）
- CCB = 默认编辑方，Codex = review/验证方；测试记录放 `tests/docs/`

## 修复：B 号 bug（macOS stop 静默泄漏）

分支 `fix/tunnel-stop-macos-verify`，commit `6fae211`（scoped，仅 2 个 .rs）。

- `commands/tunnel.rs::stop()` 不再 inline 读 `/proc`，改调
  `transport::tunnel::classify_ssh_pid`（复用 `verify_ssh_pid` 同套 `ProcessIdentity` 机制，
  跨平台），返回三态 `PidVerdict::{VerifiedSsh, Gone, NotVerifiable}`。
- 纯决策函数 `plan_stop(verdict, force) -> StopAction::{Signal, ClearStale, Refuse}`
  可跨平台测试；状态文件仅在决策后 `TunnelState::clear()`，`Refuse` 分支在 clear 前 return
  保留状态。`--force` 仍保留覆盖语义（Signal + 清状态）。
- Windows `signal_pid` 不再 `let _ =` 吞掉 taskkill 失败。
- 测试：+2 `classify_ssh_pid`（dead->Gone / 活非 ssh->NotVerifiable）+5 `plan_stop` 三态映射。
  commands::tunnel 22 passed / transport::tunnel::tests 23 passed。
- clippy `--all-targets -D warnings` / fmt / `git diff --check` 全过。

### 全量测试的 2 个 flaky（与本轮无关，已验证）
- `daemon_lifecycle::tests::different_executable_is_stale` /
  `identity::tests::macos_start_time_is_plausible_and_agrees_with_ps_when_available`
  在 macOS 沙箱并行负载下偶发 panic（`ProcessIdentity::current().expect()` 返回 Err）。
- 验证法：把本次 2 个 .rs 改动 `git stash push` 掉，在干净基线 `806b662` 跑同样两模块
  仍 FAILED（这次是 `dead_process_is_stale` + `different_executable_is_stale`）。
  → 是既有环境 flaky（沙箱偶发 EPERM 挡 `ps`/`sysctl`），非本次引入。
  ⚠️ 我的 `classify_reports_alive_non_ssh_as_not_verifiable` 也 `.unwrap()` `current()`，
  同样会在沙箱负载下偶发 panic —— 已知，与既有测试同源，非逻辑 bug。

### 仍未做的（同本轮清单 A/D/E，未排期）
- A：CLI stop 尚未接入统一 `stop_saved_tunnel()`（8/30 的 daemon_lifecycle/identity 在 CLI 仍死代码）
- D 的远端 cleanup 一致性（`:72-83` 清状态前无条件远端 rm -rf，与实施方案 :48 冲突）未处理
- `backend::open_transport()` 仍未接生产路径（Task 3）

## 完成：A 号（CLI stop 接入统一 stop_saved_tunnel）

分支 `fix/tunnel-stop-macos-verify`，commit `e57caab`（scoped：3 个 .rs，+273/-266）。

- `commands/tunnel.rs::stop()` 删掉重复的 `StopAction` / `plan_stop` / `signal_pid`
  及远端 cleanup 块 + 5 个 `plan_stop_*` 测试，现在只调
  `crate::transport::tunnel::stop_saved_tunnel(&cfg, &state, force)?`。
- `SSHClient::stop()` 改为委托 `stop_saved_tunnel`（`Some(s) => stop_saved_tunnel(&cfg, &s, false)`，
  `None => Err(NotFound)`）。
- 新增纯决策 `decide_stop(state, force) -> StopDecision` 与权威路径
  `stop_saved_tunnel(cfg, state, force) -> Result<()>`（顶替旧 `stop_decision` /
  `openssh_stop_decision`）。策略（P1 Task 2）：
  * native daemon（记录了 OS 身份）→ `verdict_to_decision(daemon_lifecycle::assess(...))`；
    `--force` **不**绕过身份校验（仍走 assess）。
  * 无身份/OpenSSH → `classify_ssh_pid`：Gone→清状态；NotVerifiable→拒绝并保留状态；
    VerifiedSsh→发信号；`--force` 可发信号（绕过 ssh 校验）。
  * pid==0 → 直接 `Conflict` 拒绝。
  * 远端 cleanup 仅在 `may_clear`（裁决授权清除）后执行 —— **D 号（清状态前无条件 rm -rf）随命令层删除一并解决**。
- 删除已无调用方的 `TunnelState::clear()`（旧命令层 `stop()` 是最后调用方；`clear_with_profile` 仍是统一路径）。
- 测试：transport::tunnel 模块 44 passed；新增 `stop_saved_tunnel_*`(3) + `decide_stop`(2)，
  并把 3 个 native 测试迁到 `verdict_to_decision(daemon_lifecycle::assess(...))`、2 个 OpenSSH fallback 迁到 `decide_stop`。

### 编译/门禁坑（已填）
- `decide_stop` 模块私有，测试 mod 的 `use super::{...}` 漏导入 → 补 `decide_stop,`。
- Rust 1.96 新 lint `clippy::zombie_processes`：spawn 的 `/bin/sleep` 必须 `.wait()` 回收，
  否则 `-D warnings` 失败；测试结尾补 `let _ = child.kill(); let _ = child.wait();`。
- `stop_saved_tunnel_refuses_unverifiable_live_process` 初版误写 `unwrap_err()`（期望
  `Conflict`）；实际"拒绝"是 graceful no-op 返回 `Ok(())`，错误只在 pid==0 / signal 失败。
  改为 spawn 真实 child + 断言 stop 后进程仍存活（`ProcessIdentity::of_pid(pid).is_ok()`）证明未发信号。

### 门禁结果
- fmt --check / clippy `-all-targets -D warnings` / git diff --check 全过。
- 全量 `cargo test`：840 passed，1 flaky（`daemon_lifecycle::live_matching_process_is_unresponsive_but_identified`），
  隔离运行该测试通过 —— 同 8/30 既有沙箱 EPERM flake，与本改动无关。

### 剩余
- E：`backend::open_transport()` 仍未接生产路径（Task 3）。
- 工作树另有未提交改动：`README.md`（+65 行 SSH 配置文档，非本轮）、`.claude/`、`.workbuddy/`、
  `docs/debug-log-2026-08-31.md`、`tests/docs/`（进度笔记）—— 均未纳入本 commit。

## 发布：v1.1.0 → crates.io（已上线）

- commit `d67c6f6`「chore(release): v1.1.0」：`Cargo.toml` 1.0.0→1.1.0 +
  `CHANGELOG.md` 补 1.1.0 条目（及此前漏记的 1.0.0 标记）。仅这 2 个文件。
- `cargo publish --allow-dirty`（README.md 等无关改动未提交，故需 --allow-dirty）
  → **Published virtuoso-cli v1.1.0**，206 files / 2.8MiB（压缩 923.9KiB）。
- crates.io API 复核：`max_version: 1.1.0`，未 yank，2026-08-31T05:12:26Z。
- token 仅以 `VAR=x cargo publish` 变量前缀传入（不进 argv、不写
  `~/.cargo/credentials.toml` —— 已验证 `NO_CRED_FILE`）。**该 token 出现在对话历史里，需 rotate。**
- `git push origin v1.1.0` + 分支均成功（网络恢复）。

### ⚠️ 发布内容缺口（重要，待决策）
tag 打在 feature 分支 `fix/tunnel-stop-macos-verify` 上，而 `origin/main` 已前进到 `52c273e`，
**领先 9 个 commit**（PR #12/#13/#14/#15 已被上游合入）。因此 crates.io 上的 1.1.0
**不含**这 9 个提交：
- 新模块 `src/transport/session_discovery.rs`（+303，bash /dev/tcp 探测取代 ss）
- `tunnel attach` / `detach` 子命令（`commands/tunnel.rs` +224、`main.rs` +30、`models.rs` +35）
- `resources/ramic_bridge.il` RBsshPort 修复（移除 unless(boundp) 守卫、fallback 改 22）
- README 4 动词隧道模型文档
差异合计 8 文件 / +733 −393。且两边都重写过 `transport/tunnel.rs`（main 侧 −504），
**合并会有实质冲突**。
处置选项：(a) yank 1.1.0，合 main 后发 1.1.1；(b) 保留 1.1.0，尽快合 main 再发 1.1.1。未决。
- GitHub Release 尚未创建（仓库惯例每个 tag 都有 Release，v1.0.0 是 Latest）。`gh` 2.78.0 可用。

## 完成：合并 main + 发布 v1.1.1（用户 "yes go"）

### 合并
- `git merge --no-ff fix/tunnel-stop-macos-verify` 到 main（main 当时已 ff 到 `52c273e`，
  README.md 先 `git stash push` 保护，避免 reset 误丢；事后 `git stash apply` 还原并 drop）。
- 唯一冲突文件 `src/commands/tunnel.rs` 为**语义冲突**（非文本）：main 新增 attached/deployed
  `mode` 概念，attached 的 daemon 属 Virtuoso 绝不能 rm -rf 远端。解法：
  * `stop_saved_tunnel` 远端清理加 `TUNNEL_MODE` 门控（attached→跳过，deployed+裁决授权清除→才清）。
  * 恢复 `TunnelState::clear()`（main 的 `detach()` 是新调用方，不再是死代码）。
- 合并 commit `a27b1ae`。门禁：fmt/clippy -D warnings/全量 test（同族 EPERM flake，已确认无关）全过。

### 发布 v1.1.1
- commit `0f3c645`「chore(release): v1.1.1」：`Cargo.toml` 1.1.0→1.1.1 + `CHANGELOG.md` 补 1.1.1
  条目（说明 fold-in 的 9 个 main 提交 + attached/deployed 门控修复）。
- `cargo publish` → **Published virtuoso-cli v1.1.1**（207 files / 2.9MiB）。
- `git tag -a v1.1.1` + 推送 main 与 tag 均成功。
- `gh release create v1.1.1 --notes-file`（CHANGELOG 1.1.1 段）→ 已建 Release。
- crates.io 复核：`max_version: 1.1.1`，未 yank。

### 🔴 凭证文件格式坑（重要，可复用）
- 手写 `~/.cargo/credentials.toml` 只放 `[registries.crates-io] token` 时，`cargo publish` 报
  "no token found" —— **cargo 1.96 把默认 crates.io token 放在 `[registry] token` 段**，
  `[registries.crates-io]` 仅用于备用 registry。**正确完整格式**：
  ```toml
  [registries.crates-io]
  token = "..."
  [registry]
  token = "..."
  ```
- 可靠写法：用 `printf '%s' '<token>' | cargo login` 让它自己写（会补 `[registry]` 段）。
  环境变量 `CARGO_REGISTRY_TOKEN` 始终可用（1.1.0/1.1.1 都靠它上传成功）。
- 本机 `~/.cargo/credentials.toml` 现已含完整两段，权限 600，`cargo publish` 不再需 env var。

### 工作树残留（非本轮）
- `README.md`（+65 行 SSH 配置文档，未提交）、`.claude/`、`docs/debug-log-2026-08-31.md`、
  `tests/docs/`、`stash@{0}`（dev 分支，无关）—— 均不影响 release。

## 完成：README SSH 配置文档（commit `e683cb5`，已推 main）

草稿是 +65 行中文段落，落地时改成 +164 行**双语各一份**（英文区 `### Configuration` 之后、
中文区 `### 配置说明` 之后），并对照代码核实内容。

### 🔴 草稿里查出的 2 处事实错误（照做必失败）
1. **`VB_SSH_CMD` 不存在**。全仓只有 `VB_SSH_PORT` / `VB_SSH_KEY` / `VB_SSH_CONFIG` /
   `VB_SSH_BACKEND`（`config.rs:244-247`）。指定密钥应用 `VB_SSH_KEY`
   → `ssh.rs:722-723` 转成 `ssh -i <key>`；`VB_SSH_CONFIG` → `-F <path>`。
2. **`VB_REMOTE_HOST=user@host` 与 `VB_REMOTE_USER=user` 不能同时给**。
   `remote_target()`（`ssh.rs:741-744`）是 `format!("{u}@{}", self.host)`，
   两者并存拼出 `user@user@host`。二者只用其一。

### 其他处置
- `StrictHostKeyChecking no` → `accept-new`（首次信任、之后变更即拒，保留 MITM 防护，
  与项目 `host_keys` 校验姿态一致），注明仅受信内网可降级为 `no`。
- 补三个真实但两份环境变量表都漏记的变量：`VB_SSH_KEY` / `VB_SSH_CONFIG` / `VB_SSH_BACKEND`。
- 已实测正确、保留：`vcli tunnel start -v`（`-v` 是 clap **全局**标志 `main.rs:68`
  `#[arg(long, short, global = true)]`，故不出现在子命令 `--help` 的 Options 里但可用，
  实测输出 DEBUG）、`vcli skill exec` / `session list`。

### 文档类改动的校验手法（可复用）
- 写进仓库的文档里每个 env var / 子命令 / flag 都要回代码核实，别信草稿。
- markdown post-edit 校验：代码围栏计数配平（本次 84 个）、双语表格行数与列数对称
  （各 16 行、每行 4 个 `|`）、`git diff --check` 查空白。

### ⚠️ 沙箱陷阱（本会话第二次踩）
**Bash 工具里的 `grep` 会静默返回空**（明明有匹配）。必须用专用 Grep 工具。
本次先用 `grep -n "VB_SSH_KEY..." README.md` 得到空结果，差点误判「README 未记录这些变量」。

## 完成：E 号（Task 3 — 真实后端选择接入生产路径）

commit `b82140b`（main，已推 origin/main `e683cb5..b82140b`），9 文件 +94/−27。

### 改动
- **新增 `backend::require_openssh(config)`**（`transport/backend.rs`）：native→
  `TransportError::UnsupportedBackend`，openssh/unset→Ok。供 OpenSSH-only 路径调用。
- **4 个动态 trait 消费者 → `open_transport`**（native 现在会真正失败，不再静默回退）：
  `diag.rs` / `sim.rs::check_license` / `maestro.rs::create_corner_netlist` /
  `x11.rs::transport_for_config`（后者统一覆盖 window.rs 全部 X11 路径；
  `open_transport` 已处理 `disable_control_master`，行为不变）。
- **OpenSSH-only 路径加 guard**（拒绝显式 native，本地命令不受影响）：
  `tunnel.rs::SSHClient::from_env`（覆盖 tunnel/bridge/session 专属子进程）、
  `spectre/runner.rs::SpectreSimulator::from_env` 的 remote 分支（本地 spectre 不触 backend）、
  `skill.rs::find` 远程分支 + `sync_cache`（覆盖 ssh + scp 完整同步路径）。
- `backend.rs` 移除 `#![allow(dead_code)]`（`open_transport` 现已消费）；
  仅给 `supported_in_this_build` 保留 `#[allow(dead_code)]`（future native-ssh 增量用）。
- **顺带修 `resources/ramic_bridge.il` RB_VERSION 戳 1.0.0→1.1.1**：对齐 1.1.1 发布的
  `Cargo.toml`，否则 `ramic_bridge_version_stamp_matches_cargo_toml` 红（与本次迁移无关，
  是 1.1.1 bump 遗留，已并入本 commit 让门禁绿）。

### 测试
- 新增 `backend::require_openssh` 三条（default/openssh 通过、native 拒绝、unknown→Configuration）
  + `tunnel::SSHClient::from_env` 拒绝 native（env 锁 + 还原）。
- 🔴 **Rust 1.96 两个新 lint 坑**（可复用）：
  1. `clippy::err_expect`：`.err().expect(msg)` 必须改 `.expect_err(msg)`。
  2. 但 `.expect_err()` / `.unwrap_err()` **要求 Ok 类型 `Debug`**；
     `Arc<dyn RemoteTransport>` / `SSHClient` 都非 Debug → 这两种都用不了。
     非 Debug T 的结果改用 `match { Ok(_) => panic!(..), Err(e) => e }`。

### 门禁（全过）
- fmt --check / clippy `--all-targets -D warnings` / git diff --check。
- backend 13 测试 + SSHClient 1 测试通过；全量 `cargo test` 除既有
  `daemon_lifecycle::live_matching_process_is_unresponsive_but_identified` flake
  （隔离 3/3 通过、本次未触碰该模块）外全绿。

### 工作树未提交项（非本轮，排除）
- `.claude/skills/tunnel-connect/SKILL.md`（外部改动，+53，未纳入）、
  `.claude/settings.local.json`、`docs/debug-log-2026-08-31.md`、`tests/docs/`。

### 本轮 transport P1 收尾状态
- Task 1（host_keys）已在 806b662→52c273e 的 main 提交里（早于本轮）。
- Task 2（stop 统一路径）= A 号，已合 main + 发 1.1.1。
- Task 3（真实后端选择）= E 号，本次完成并推 main。
- 下一步仅剩 native 直连客户端（依赖 `cargo add russh`，P0 #1，尚未做）。
